FROM python:3.12-slim

# Install MLflow >= 3.0.0 and PostgreSQL backend driver
RUN pip install --no-cache-dir "mlflow>=3.0.0" psycopg2-binary

# Expose port for MLflow UI
EXPOSE 5000

# Set entrypoint to mlflow server
ENTRYPOINT ["mlflow", "server"]
