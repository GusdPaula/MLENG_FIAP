FROM python:3.12-slim

# Install MLflow >= 3.0.0, PostgreSQL backend driver, and boto3 for S3 storage
RUN pip install --no-cache-dir "mlflow>=3.0.0" psycopg2-binary boto3

# Expose port for MLflow UI
EXPOSE 5000

# Set entrypoint to mlflow server
ENTRYPOINT ["mlflow", "server"]
